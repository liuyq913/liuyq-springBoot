# security:
#  basic:
#    enabled: true
#  user:
#    name: admin
#    password: admin

server:
  port: 8082

spring:
   application:
     name: boot-consumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/